export default class ResData {
    constructor(public no: number,
    public mail: string,
    public name: string,
    public date: string,
    public id: string,
    public message: string) {
    }
}
